name: example
description: An example app for flutter_buttombar_chung

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_buttombar_chung:
    path: ../ # 這裡是你的套件的相對路徑

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true